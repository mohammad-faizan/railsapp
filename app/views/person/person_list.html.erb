<h1><%= params[:type].capitalize.pluralize %> Record List</h1>
<div class="row">
  <div class="col text-start">
      <%= link_to "<< Back", root_path, class: "btn btn-secondary" %>
  </div>

  <div class="col text-end">
      <%= link_to "New #{params[:type].capitalize}", new_path(type: params[:type]), class: "btn btn-primary" %>
  </div>
</div>

<%= display_people_table(@list) %>
